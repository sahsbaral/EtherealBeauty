export default function postForm<T>(path: string, params: {
    [key in keyof T]: string;
}): void;
type ConversionProps<T> = {
    [key in keyof T]: string | number;
};
type StringifiedObj<T> = {
    [key in keyof T]: string;
};
export declare function convertObjectDataToString<T>(paramsData: ConversionProps<T>): StringifiedObj<T>;
export {};
