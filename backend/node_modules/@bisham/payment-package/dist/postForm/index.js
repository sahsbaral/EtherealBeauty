"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.convertObjectDataToString = void 0;
function postForm(path, params) {
    if (typeof window === 'undefined') {
        throw new Error('Cannot Initiate Payment In Node Environment. Run this Method in Brower Environment.');
    }
    let form = document.createElement('form');
    form.setAttribute('method', 'POST');
    form.setAttribute('action', path);
    for (let key in params) {
        let hiddenField = document.createElement('input');
        hiddenField.setAttribute('type', 'hidden');
        hiddenField.setAttribute('name', key);
        hiddenField.setAttribute('value', params[key]);
        form.appendChild(hiddenField);
    }
    document.body.appendChild(form);
    form.submit();
}
exports.default = postForm;
function convertObjectDataToString(paramsData) {
    let stringifiedObjectValues = {};
    for (let key in paramsData) {
        stringifiedObjectValues[key] = paramsData[key].toString();
    }
    return stringifiedObjectValues;
}
exports.convertObjectDataToString = convertObjectDataToString;
